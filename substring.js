const str = 'Have a nice day';
const checkStr = 'nice';

if(str.includes(checkStr)){
    console.log(`The string has '${checkStr}' in it.`);
}
else{
    console.log(`The string does not contain '${checkStr}' in it.`);
}
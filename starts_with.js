const str1 = 'Javascript is a programming language';
const str2 = 'java';

//check string starts with another string
if(str1.toLowerCase().startsWith(str2)){
    console.log(`The string starts with ${str2}`);
}
else{
    console.log(`The string does not start with ${str2}`);
}

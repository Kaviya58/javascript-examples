//check if an object is array or not

const arr = [1,2,3,4,5];

let result = Array.isArray(arr);

if(result){
    console.log(`${arr} is an array.`);
}
else{
    console.log(`${arr} is an array.`);
}
